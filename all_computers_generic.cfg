#### 1. Localization & Keyboard
d-i debian-installer/locale string en_US.UTF-8
d-i localechooser/supported-locales multiselect el_GR.UTF-8, en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us
d-i keyboard-configuration/layoutcode string us,gr
d-i keyboard-configuration/toggle select Alt+Shift

#### 2. Network & Clock
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian-pro
d-i time/zone string Europe/Athens

#### 3. Mirror & Firmware (Crucial for Nvidia)
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i apt-setup/non-free-firmware boolean true
d-i apt-setup/non-free boolean true
d-i apt-setup/contrib boolean true

#### 4. Partitioning (Encrypted LVM - PW "1")
d-i partman-auto/method string crypto
d-i partman-crypto/passphrase password 1
d-i partman-crypto/passphrase-again password 1
d-i partman-crypto/weak_passphrase boolean true
d-i partman-auto/choose_recipe select atomic
d-i partman-auto-lvm/guided_size string max
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

#### 5. User Accounts
d-i passwd/root-password password 1
d-i passwd/root-password-again password 1
d-i passwd/user-fullname string m
d-i passwd/username string m
d-i passwd/user-password password 1
d-i passwd/user-password-again password 1
d-i user-setup/allow-password-weak boolean true

#### 6. Software Selection (The Utilitarian Pro Stack)
tasksel tasksel/first multiselect standard
d-i pkgsel/recommends boolean false
d-i pkgsel/include string \
  xfce4-session xfce4-panel xfwm4 xfdesktop4 xfce4-terminal xfce4-settings \
  thunar mousepad firefox-esr lightdm xserver-xorg \
  nvidia-driver nvidia-settings nvidia-vaapi-driver \
  libvdpau-va-gl1 vdpau-driver-all intel-microcode \
  firmware-linux-nonfree firmware-misc-nonfree firmware-realtek firmware-iwlwifi \
  sudo pipewire pipewire-audio wireplumber network-manager-gnome polkit-1-gnome \
  xfce4-power-manager gvfs-backends udisks2 usbutils pciutils linux-cpupower \
  xfburn arandr gnome-disk-utility smartmontools fuse3 nvtop vainfo \
  exfatprogs dosfstools ntfs-3g wireless-tools wpasupplicant bluetooth bluez

#### 7. Post-Install (4K & Performance)
d-i preseed/late_command string \
    in-target sed -i 's/#autologin-user=/autologin-user=m/' /etc/lightdm/lightdm.conf; \
    in-target adduser m sudo; \
    in-target adduser m video; \
    in-target adduser m render; \
    in-target nvidia-xconfig; \
    in-target bash -c 'echo "LIBVA_DRIVER_NAME=nvidia" >> /etc/environment'; \
    in-target bash -c 'echo "NVD_BACKEND=direct" >> /etc/environment'; \
    in-target bash -c 'echo "MOZ_DISABLE_RDD_SANDBOX=1" >> /etc/environment'; \
    in-target bash -c 'echo "VDPAU_DRIVER=nvidia" >> /etc/environment'; \
    in-target bash -c 'printf "#!/bin/sh -e\ncpupower frequency-set -g performance\nexit 0" > /etc/rc.local'; \
    in-target chmod +x /etc/rc.local; \
    in-target systemctl enable lightdm

d-i finish-install/reboot_in_progress note

