# Debian 13 (Trixie) Preseed Configuration - Optimized for VirtualBox
# Locale & Keyboard
d-i debian-installer/locale string en_US.UTF-8
d-i localechooser/supported-locales multiselect en_US.UTF-8, el_GR.UTF-8
d-i keyboard-configuration/xkb-keymap select us
d-i keyboard-configuration/layoutcode string us,gr
d-i keyboard-configuration/optionscode string grp:alt_shift_toggle

# Network Configuration
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian-vm
d-i netcfg/get_domain string local
d-i netcfg/dhcp_timeout string 60

# Mirror Settings (Greece)
d-i mirror/country string Greece
d-i mirror/http/hostname string ftp.gr.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string
d-i mirror/suite string trixie

# User Account
d-i passwd/user-fullname string m
d-i passwd/username string m
d-i passwd/user-password password 1
d-i passwd/user-password-again password 1
d-i user-setup/allow-password-weak boolean true
d-i user-setup/encrypt-home boolean false
d-i passwd/root-login boolean false

# Clock & Timezone
d-i clock-setup/utc boolean true
d-i time/zone string Europe/Athens

# Partitioning (Full Disk Encryption)
d-i partman-auto/method string crypto
d-i partman-crypto/passphrase string 1
d-i partman-crypto/passphrase-again string 1
d-i partman-auto-lvm/guided_size string max
d-i partman-auto/choose_recipe select atomic
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# APT Repositories
d-i apt-setup/non-free-firmware boolean true
d-i apt-setup/non-free boolean true
d-i apt-setup/contrib boolean true

# Package Selection - Comprehensive XFCE Desktop + Tools
tasksel tasksel/first multiselect
d-i pkgsel/include string \
    sudo \
    lightdm lightdm-gtk-greeter \
    xfce4-session xfwm4 xfce4-panel xfdesktop4 xfce4-terminal \
    xfce4-settings xfce4-whiskermenu-plugin xfce4-power-manager xfce4-notifyd \
    thunar thunar-archive-plugin thunar-media-tags-plugin \
    mousepad geany \
    network-manager-gnome \
    pulseaudio pavucontrol \
    gvfs gvfs-backends gvfs-fuse \
    dbus-x11 polkitd \
    upower acpi-support acpid \
    firmware-linux-nonfree \
    virtualbox-guest-x11 virtualbox-guest-utils virtualbox-guest-dkms \
    udisks2 usbutils usb-modeswitch \
    curl wget git \
    firefox-esr \
    qbittorrent vlc \
    flameshot \
    papirus-icon-theme bibata-cursor-theme \
    fonts-inter fonts-jetbrains-mono fonts-dejavu fonts-noto-color-emoji \
    locales-all \
    htop neofetch \
    xdg-user-dirs xdg-utils \
    gnome-keyring ssh-askpass

d-i pkgsel/upgrade select full-upgrade

# Bootloader
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default
d-i grub-installer/timeout string 5

# Post-Installation Configuration
d-i preseed/late_command string \
    in-target usermod -aG sudo,netdev,plugdev,vboxsf,input,audio,video m; \
    in-target localectl set-x11-keymap us,gr "" "" grp:alt_shift_toggle; \
    in-target dbus-launch dconf write /org/xfce/xfconf/xfce4/xsettings/Gtk/IconThemeName "'Papirus-Dark'" || true; \
    in-target dbus-launch dconf write /org/xfce/xfconf/xfce4/xsettings/Gtk/CursorThemeName "'Bibata-Modern-Classic'" || true; \
    in-target mkdir -p /etc/lightdm/lightdm.conf.d/; \
    in-target bash -c 'cat > /etc/lightdm/lightdm.conf.d/01-autologin.conf' << 'LIGHTDM_CONF' \
[Seat:*] \
autologin-user=m \
autologin-user-timeout=0 \
session=xfce \
LIGHTDM_CONF \
; \
    in-target mkdir -p /etc/X11/xorg.conf.d/; \
    in-target bash -c 'cat > /etc/X11/xorg.conf.d/40-libinput.conf' << 'XORG_CONF' \
Section "InputClass" \
 Identifier "touchpad" \
 MatchIsTouchpad "on" \
 Option "Tapping" "on" \
 Option "TappingDrag" "on" \
 Option "NaturalScrolling" "false" \
EndSection \
XORG_CONF \
; \
    in-target bash -c 'cat > /etc/X11/xorg.conf.d/30-keyboard.conf' << 'KEYBOARD_CONF' \
Section "InputClass" \
 Identifier "keyboard" \
 MatchIsKeyboard "on" \
 Option "XkbLayout" "us,gr" \
 Option "XkbOptions" "grp:alt_shift_toggle" \
EndSection \
KEYBOARD_CONF \
; \
    in-target mkdir -p /home/m/.local/share/xfce4/terminal/; \
    in-target bash -c 'cat > /home/m/.local/share/xfce4/terminal/terminalrc' << 'TERM_CONF' \
[Configuration] \
FontName=JetBrains Mono 11 \
Scrollbar=SCROLLBAR_RIGHT \
ColorPalette=rgb(39,40,34);rgb(249,38,114);rgb(166,226,46);rgb(253,151,31);rgb(102,217,239);rgb(174,129,255);rgb(161,239,228);rgb(248,248,242) \
MiscBordersDefault=true \
MiscCursorBlinks=true \
MiscDefaultGeometry=100x30 \
TERM_CONF \
; \
    in-target chown -R m:m /home/m/.local/; \
    in-target chown -R m:m /home/m/.config/ || true; \
    in-target xdg-user-dirs-update --force; \
    in-target systemctl enable vboxadd-service || true; \
    in-target systemctl enable NetworkManager || true

# Finish
d-i finish-install/reboot_in_progress note
